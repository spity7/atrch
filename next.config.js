/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "standalone",
  // basePath: "/pekko-nextjs",
  // assetPrefix: "/pekko-nextjs/",
  trailingSlash: true,
};

module.exports = nextConfig;
